# Optional: Gemini API key (only needed if Gemini-backed features are enabled in your fork).
GEMINI_API_KEY=""

# Optional: public URL where the app is served (callbacks, canonical links).
APP_URL="http://localhost:5173"
