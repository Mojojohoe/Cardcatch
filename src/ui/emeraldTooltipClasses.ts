/**
 * Shared panel styling for emerald “strip” tooltips (panic dice, shop afford hints, etc.).
 * Positioning (`absolute`, offsets, `max-w-*`) stays at call sites.
 */
export const EMERALD_STRIP_TOOLTIP_PANEL =
  'rounded-xl border border-slate-600/65 bg-emerald-950/98 px-3 py-2.5 text-left text-[11px] font-semibold leading-snug text-emerald-50 shadow-xl backdrop-blur-sm';
