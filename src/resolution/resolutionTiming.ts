/** Round resolution: empower ladder — pause on each rank, then wiggle (see `ResolutionSequence` CARD_EMPOWER). */
export const RESOLUTION_UPGRADE_INTRO_MS = 320;
export const RESOLUTION_UPGRADE_PAUSE_MS = 200;
export const RESOLUTION_UPGRADE_WIGGLE_MS = 380;
