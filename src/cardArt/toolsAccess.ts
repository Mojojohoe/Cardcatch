/**
 * Card-art editing is intentionally enabled in this branch/build:
 * Card Creator UI is visible and must persist edits to localStorage + room sync.
 */
export const CARD_ART_TOOLS_ENABLED = true;
